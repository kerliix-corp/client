<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Dashboard</title>
    <style>
      body { font-family: Arial, sans-serif; margin: 40px; }
      pre { background: #f5f5f5; padding: 20px; border-radius: 5px; overflow-x: auto; }
      a { display: inline-block; margin-top: 20px; padding: 10px 20px; background-color: #dc3545; color: #fff; text-decoration: none; border-radius: 5px; }
      a:hover { background-color: #b02a37; }
    </style>
  </head>
  <body>
    <h1>Dashboard</h1>
    <h2>Logged in user info:</h2>
    <pre><%= JSON.stringify(user, null, 2) %></pre>
    <a href="/logout">Logout</a>
  </body>
</html>
